---
phase: 03-cache-and-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/br/com/precatorios/config/CacheConfig.kt
  - src/main/kotlin/br/com/precatorios/scraper/EsajScraper.kt
  - src/main/kotlin/br/com/precatorios/scraper/CacScraper.kt
  - src/main/kotlin/br/com/precatorios/scraper/DataJudClient.kt
  - src/test/kotlin/br/com/precatorios/config/CacheConfigTest.kt
  - src/main/resources/application.yml
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02

must_haves:
  truths:
    - "A second identical call to fetchProcesso() within 24h does not invoke the underlying scraper HTTP call"
    - "A second identical call to fetchPrecatorio() within 24h does not invoke the underlying scraper HTTP call"
    - "A second identical call to buscarPorNumeroProcesso() within 24h does not invoke the underlying scraper HTTP call"
    - "Cache is keyed by the single String parameter (numero) so two callers looking up the same number share the cached result"
    - "buscarPorNome and buscarPorMunicipio are NOT cached"
    - "A scraper method returning null does not populate the cache (no negative caching)"
  artifacts:
    - path: "src/main/kotlin/br/com/precatorios/config/CacheConfig.kt"
      provides: "@EnableCaching + CaffeineCacheManager with three named caches"
      contains: "@EnableCaching"
    - path: "src/test/kotlin/br/com/precatorios/config/CacheConfigTest.kt"
      provides: "Integration test proving cache hit on second call"
      contains: "verify(exactly = 1)"
  key_links:
    - from: "src/main/kotlin/br/com/precatorios/scraper/EsajScraper.kt"
      to: "CacheConfig"
      via: "@Cacheable(cacheNames = [CacheNames.PROCESSOS])"
      pattern: "@Cacheable.*processos"
    - from: "src/main/kotlin/br/com/precatorios/scraper/CacScraper.kt"
      to: "CacheConfig"
      via: "@Cacheable(cacheNames = [CacheNames.PRECATORIOS])"
      pattern: "@Cacheable.*precatorios"
    - from: "src/main/kotlin/br/com/precatorios/scraper/DataJudClient.kt"
      to: "CacheConfig"
      via: "@Cacheable(cacheNames = [CacheNames.DATAJUD])"
      pattern: "@Cacheable.*datajud"
---

<objective>
Wire Caffeine read-through caching to all three scraper fetch methods with 24h TTL, no negative caching, and cache keys by process/precatorio number.

Purpose: Avoid redundant HTTP requests to TJ-SP portals during BFS prospection (Phase 4) and repeated lookups via REST endpoints (Phase 2).
Output: CacheConfig bean, @Cacheable annotations on three scraper methods, integration test proving cache behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cache-and-scoring/03-CONTEXT.md
@.planning/phases/03-cache-and-scoring/03-RESEARCH.md

@src/main/kotlin/br/com/precatorios/config/ScraperProperties.kt
@src/main/kotlin/br/com/precatorios/config/ResilienceConfig.kt
@src/main/kotlin/br/com/precatorios/scraper/EsajScraper.kt
@src/main/kotlin/br/com/precatorios/scraper/CacScraper.kt
@src/main/kotlin/br/com/precatorios/scraper/DataJudClient.kt
@src/main/resources/application.yml

<interfaces>
<!-- Key types the executor needs from existing scraper methods -->

From src/main/kotlin/br/com/precatorios/scraper/EsajScraper.kt:
```kotlin
fun fetchProcesso(numero: String): ProcessoScraped { ... }
fun buscarPorNome(nome: String): List<BuscaResultado> { ... }  // NOT cached per D-08
```

From src/main/kotlin/br/com/precatorios/scraper/CacScraper.kt:
```kotlin
fun fetchPrecatorio(numero: String): PrecatorioScraped { ... }
```

From src/main/kotlin/br/com/precatorios/scraper/DataJudClient.kt:
```kotlin
fun buscarPorNumeroProcesso(numero: String): DataJudResult { ... }
fun buscarPorMunicipio(codigoIbge: String): DataJudResult { ... }  // NOT cached per D-09
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CacheConfig with CaffeineCacheManager and three named caches</name>
  <files>
    src/main/kotlin/br/com/precatorios/config/CacheConfig.kt
    src/main/resources/application.yml
  </files>
  <read_first>
    src/main/kotlin/br/com/precatorios/config/ResilienceConfig.kt
    src/main/kotlin/br/com/precatorios/config/ScraperProperties.kt
    src/main/resources/application.yml
  </read_first>
  <action>
Create `src/main/kotlin/br/com/precatorios/config/CacheConfig.kt`:

1. Define a `CacheNames` object with three constants:
   - `const val PROCESSOS = "processos"`
   - `const val PRECATORIOS = "precatorios"`
   - `const val DATAJUD = "datajud"`

2. Create a `@Configuration @EnableCaching` class `CacheConfig` with a `@Bean fun cacheManager(): CacheManager`:
   - Use `CaffeineCacheManager(CacheNames.PROCESSOS, CacheNames.PRECATORIOS, CacheNames.DATAJUD)` — the simpler approach since all three caches share the same 24h TTL (per D-10)
   - Call `manager.setCaffeine(Caffeine.newBuilder().expireAfterWrite(24, TimeUnit.HOURS).maximumSize(1000).recordStats())`
   - Return the manager

3. Imports needed:
   - `com.github.ben-manes.caffeine.cache.Caffeine`
   - `org.springframework.cache.CacheManager`
   - `org.springframework.cache.annotation.EnableCaching`
   - `org.springframework.cache.caffeine.CaffeineCacheManager`
   - `org.springframework.context.annotation.Bean`
   - `org.springframework.context.annotation.Configuration`
   - `java.util.concurrent.TimeUnit`

4. Update `src/main/resources/application.yml` — add `caches` endpoint to actuator exposure for monitoring:
   - Change `include: health` to `include: health,caches` under `management.endpoints.web.exposure`

Per D-10: three named Caffeine caches with 24h TTL. maximumSize=1000 is Claude's discretion.
  </action>
  <verify>
    <automated>cd /home/jucassoli/development/git-repo/prec-poc1 && ./gradlew compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CacheConfig.kt contains `@EnableCaching`
    - CacheConfig.kt contains `@Configuration`
    - CacheConfig.kt contains `object CacheNames` with `const val PROCESSOS = "processos"`, `const val PRECATORIOS = "precatorios"`, `const val DATAJUD = "datajud"`
    - CacheConfig.kt contains `@Bean` and `fun cacheManager(): CacheManager`
    - CacheConfig.kt contains `CaffeineCacheManager(CacheNames.PROCESSOS, CacheNames.PRECATORIOS, CacheNames.DATAJUD)`
    - CacheConfig.kt contains `expireAfterWrite(24, TimeUnit.HOURS)`
    - CacheConfig.kt contains `maximumSize(1000)`
    - application.yml contains `include: health,caches`
    - `./gradlew compileKotlin` exits 0
  </acceptance_criteria>
  <done>CacheConfig.kt compiles, defines three named caches with 24h TTL, @EnableCaching active, actuator caches endpoint exposed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add @Cacheable to three scraper methods and create cache integration test</name>
  <files>
    src/main/kotlin/br/com/precatorios/scraper/EsajScraper.kt
    src/main/kotlin/br/com/precatorios/scraper/CacScraper.kt
    src/main/kotlin/br/com/precatorios/scraper/DataJudClient.kt
    src/test/kotlin/br/com/precatorios/config/CacheConfigTest.kt
  </files>
  <read_first>
    src/main/kotlin/br/com/precatorios/scraper/EsajScraper.kt
    src/main/kotlin/br/com/precatorios/scraper/CacScraper.kt
    src/main/kotlin/br/com/precatorios/scraper/DataJudClient.kt
    src/main/kotlin/br/com/precatorios/config/CacheConfig.kt
  </read_first>
  <behavior>
    - Test 1: Calling EsajScraper.fetchProcesso("123") twice invokes the underlying HTTP logic exactly once (verify via MockK spy or mock)
    - Test 2: Calling CacScraper.fetchPrecatorio("456") twice invokes the underlying HTTP logic exactly once
    - Test 3: Calling DataJudClient.buscarPorNumeroProcesso("789") twice invokes the underlying HTTP logic exactly once
    - Test 4: Two different process numbers ("123" vs "456") invoke the HTTP logic twice (separate cache entries, per CACHE-02)
  </behavior>
  <action>
1. **EsajScraper.kt** — Add to `fetchProcesso()` method:
   - Import: `import org.springframework.cache.annotation.Cacheable` and `import br.com.precatorios.config.CacheNames`
   - Annotate: `@Cacheable(cacheNames = [CacheNames.PROCESSOS], unless = "#result == null")` on `fun fetchProcesso(numero: String): ProcessoScraped`
   - Do NOT annotate `buscarPorNome` (per D-08)

2. **CacScraper.kt** — Add to `fetchPrecatorio()` method:
   - Import: `import org.springframework.cache.annotation.Cacheable` and `import br.com.precatorios.config.CacheNames`
   - Annotate: `@Cacheable(cacheNames = [CacheNames.PRECATORIOS], unless = "#result == null")` on `fun fetchPrecatorio(numero: String): PrecatorioScraped`

3. **DataJudClient.kt** — Add to `buscarPorNumeroProcesso()` method:
   - Import: `import org.springframework.cache.annotation.Cacheable` and `import br.com.precatorios.config.CacheNames`
   - Annotate: `@Cacheable(cacheNames = [CacheNames.DATAJUD], unless = "#result == null")` on `fun buscarPorNumeroProcesso(numero: String): DataJudResult`
   - Do NOT annotate `buscarPorMunicipio` (per D-09)

4. **CacheConfigTest.kt** — Create `src/test/kotlin/br/com/precatorios/config/CacheConfigTest.kt`:
   - Use `@SpringBootTest` with `@Import(CacheConfig::class)`
   - Use `@MockkBean` to mock scraper dependencies that the scraper beans need (Resilience4j beans, ScraperProperties, WebClient.Builder) so the Spring context boots
   - Alternatively, use a focused `@TestConfiguration` that creates spy-wrapped scraper beans
   - Test pattern for each scraper:
     ```kotlin
     @Test
     fun `fetchProcesso second call returns cached result without scraper invocation`() {
         // Given: mock/spy the scraper to return a fixed result
         every { esajScraper.fetchProcesso("1234567-89.2020.8.26.0100") } returns testProcessoScraped
         // When: call twice
         val first = esajScraper.fetchProcesso("1234567-89.2020.8.26.0100")
         val second = esajScraper.fetchProcesso("1234567-89.2020.8.26.0100")
         // Then: underlying method invoked only once
         verify(exactly = 1) { esajScraper.fetchProcesso("1234567-89.2020.8.26.0100") }
         assertThat(first).isEqualTo(second)
     }
     ```
   - Include a test that two different keys produce two separate cache entries (verify exactly = 2 when different numbers used)
   - Note: The test approach depends on whether scraper beans can be spied with `@SpykBean`. If the scraper has final methods or constructor issues, an alternative is to inject the `CacheManager`, call the method twice, and assert `cacheManager.getCache("processos")?.get("the-number")` is not null after first call.

Per D-07: Only fetchProcesso, fetchPrecatorio, buscarPorNumeroProcesso are cached.
Per D-08, D-09: buscarPorNome and buscarPorMunicipio are NOT cached.
Per D-10: `unless = "#result == null"` prevents negative caching.
  </action>
  <verify>
    <automated>cd /home/jucassoli/development/git-repo/prec-poc1 && ./gradlew test --tests "*.CacheConfigTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - EsajScraper.kt contains `@Cacheable(cacheNames = [CacheNames.PROCESSOS], unless = "#result == null")` above `fun fetchProcesso`
    - CacScraper.kt contains `@Cacheable(cacheNames = [CacheNames.PRECATORIOS], unless = "#result == null")` above `fun fetchPrecatorio`
    - DataJudClient.kt contains `@Cacheable(cacheNames = [CacheNames.DATAJUD], unless = "#result == null")` above `fun buscarPorNumeroProcesso`
    - EsajScraper.kt does NOT contain `@Cacheable` on `buscarPorNome`
    - DataJudClient.kt does NOT contain `@Cacheable` on `buscarPorMunicipio`
    - CacheConfigTest.kt exists and contains at least 3 test methods
    - CacheConfigTest.kt contains `verify(exactly = 1)` or equivalent cache-hit assertion
    - `./gradlew test --tests "*.CacheConfigTest"` exits 0
  </acceptance_criteria>
  <done>Three scraper methods annotated with @Cacheable, integration test proves second call is served from cache without scraper invocation, buscarPorNome and buscarPorMunicipio confirmed NOT cached</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache store (in-process memory) | Cached scraper results live in JVM heap; no external cache service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | CaffeineCacheManager | mitigate | `maximumSize(1000)` per cache prevents unbounded memory growth from cache poisoning with unique keys |
| T-03-02 | Information Disclosure | Actuator /caches endpoint | accept | Endpoint only exposes cache names and stats (hit/miss counts), no cached values; same trust level as /health |
| T-03-03 | Tampering | Cache key (process number) | accept | Keys come from internal BFS engine and validated controller inputs (Phase 2 controllers validate format); no external user can inject arbitrary cache keys |
</threat_model>

<verification>
1. `./gradlew compileKotlin` exits 0
2. `./gradlew test --tests "*.CacheConfigTest"` exits 0
3. `grep -r "@Cacheable" src/main/kotlin/` shows exactly 3 annotations (fetchProcesso, fetchPrecatorio, buscarPorNumeroProcesso)
4. `grep -r "@EnableCaching" src/main/kotlin/` shows exactly 1 annotation (CacheConfig)
</verification>

<success_criteria>
- CacheConfig.kt defines three named Caffeine caches with 24h TTL and @EnableCaching
- Three scraper fetch methods annotated with @Cacheable and `unless = "#result == null"`
- Search methods (buscarPorNome, buscarPorMunicipio) are NOT cached
- Integration test proves second identical call does not invoke underlying scraper
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-cache-and-scoring/03-01-SUMMARY.md`
</output>
